# Prebuilt Documentation

This directory contains textual versions of pre-written GBC files found inside the
`ext/prebuilt/` directory. These files don't serve any functional purpose, only make
it easier to parse what the prebuilt GBC test files are doing.
